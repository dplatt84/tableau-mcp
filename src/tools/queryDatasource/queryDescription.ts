export const queryDatasourceToolDescription = `Executes VizQL queries against Tableau data sources to answer business questions. Retrieves aggregated filtered data with sorting grouping. BEST PRACTICES: Prefer aggregation SUM COUNT AVG over row-level data. Profile first run COUNT when unsure about volume. Use TOP filters for top N. Apply SET QUANTITATIVE DATE filters to reduce data. Avoid row-level queries unless needed. FIELD TYPES: Dimensions basic fields no aggregation. Measures aggregation functions SUM AVG COUNT. Calculated custom fields Tableau syntax. Bins group continuous data ranges use binSize. FILTERS: SET specific values. TOP top bottom N by measure. QUANTITATIVE numeric ranges. DATE relative periods. PARAMETERS: Dynamic values control calculations filters. Types LIST ANY_VALUE QUANTITATIVE_RANGE QUANTITATIVE_DATE. Include parameters array name value pairs. BINS: Create dynamically measure field and bin field same fieldCaption binSize. Cannot override existing bins query as dimensions. PROFILING: Count total then by key dimensions then aggregate or filter. Profile when all records or high-cardinality without filters. Suggest aggregation when counts exceed 10000. Do not call if fields unavailable or incompatible. Use metadata tools first.
`;
